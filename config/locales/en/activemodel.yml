en:
  activerecord:
    attributes:
      user:
        name: name
        password: password
        email: email

    errors:
      messages:
        invalid_email: isn't a valid address.
        disposable_email: can't be used (temporary email).
