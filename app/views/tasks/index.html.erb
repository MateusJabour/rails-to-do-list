<%= render 'form', task: @task, action: tasks_path %>

<%= form_tag update_tasks_path, method: 'patch' do %>
  <ul>
    <% for task in @tasks %>
    <%= render task %>
    <% end %>
  </ul>

  <p>
    <%= submit_tag t('form.buttons.update_tasks'), class: 'button' %>
  </p>
<% end %>
