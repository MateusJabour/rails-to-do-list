pt-BR:
  activerecord:
    attributes:
      user:
        name: nome
        password: senha
        email: email

    errors:
      messages:
        invalid_email: não é um endereco válido.
        disposable_email: não pode ser usado (email temporário).
